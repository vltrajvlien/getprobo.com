
---
import { filterLang } from "../../../lib/i18n.ts";
export async function getStaticPaths(param: {routePattern: string}) {
  const stories = await getCollection("stories", filterLang('fr'));
  return stories.map((item) => ({
    params: { id: item.id.split('/').at(-1) },
    props: { item },
  }));
}
import Page from '../../stories/_page.astro'
import { getCollection } from "astro:content";
---

<Page {...Astro.props} />
